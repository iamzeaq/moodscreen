/**
 * impact — free. Bebas Neue.
 *
 * The loud one. A tall condensed face fits far more statement at a far larger
 * size than anything else here, which is why its ladder starts at 50 while
 * `nokia` starts at 30 — the number sets the em box, not the letters.
 */
export const impact = {
  id: "impact",
  name: "Impact",
  tier: "free",

  font: {
    family: '"Bebas Neue", "Arial Narrow", sans-serif',
    faceFamily: "Bebas Neue",
    weight: 400,
    case: "upper",
    tracking: "0.01em",
    lineHeight: 1.05,
    scale: [50, 40, 30, 24],
  },

  texture: "glyph",
};
