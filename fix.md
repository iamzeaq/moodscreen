Two fixes, and I need to actually see the download work.

1. The name field in the hero still doesn't update the card live. The statement updates fine; the name doesn't. It's been "fixed" once already and still doesn't work. Trace why the name input isn't reaching the card, and fix it for real.

2. The guest download has never once produced a file in my browser across several fixes. Stop telling me it's verified in the terminal — I need it working in Chrome. Add a plain, obvious "Download" button to the hero, right next to the claim field, that saves the PNG. Keep "Claim yours" separate — it goes to signup, it is not the download. Make the download button impossible to miss and make it actually fire.

After both, tell me exactly which button to click and I'll test in the browser.